# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_ISSUER=reed
JWT_AUDIENCE=reed-users
JWT_REALM=reed
JWT_EXPIRATION=86400000 # 24 hours in milliseconds

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=reed
DB_USER=postgres
DB_PASSWORD=postgres

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080 